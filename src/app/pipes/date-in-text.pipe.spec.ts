import { DateInTextPipe } from './date-in-text.pipe';

describe('DateInTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DateInTextPipe();
    expect(pipe).toBeTruthy();
  });
});
